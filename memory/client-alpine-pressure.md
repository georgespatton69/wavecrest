---
name: client-alpine-pressure
description: "Alpine Pressure (pressure washing) — brand, Meta messaging A/B campaign state, IG access issue"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4d2a8846-9266-484d-a3c4-fd5e4eb1e961
---

**Alpine Pressure Wash & Seal** — pressure washing + concrete/paver/pool-deck sealing. Owner **Brandon Williams** ("born and raised in Carbondale"). Serves the entire Roaring Fork Valley (Aspen, Snowmass Village, Basalt, Willits, Carbondale, Glenwood Springs; Pitkin/Garfield/Eagle counties). Site: alpinepressure.com (Lovable/GPT-engineer SPA — images are in the JS bundle at `/assets/`, not raw HTML). Taglines: "Protecting your property one drop at a time," "No spam. No pressure. Just your free estimate," 24-hr response, 5.0 Google rating, Dayton Superior sealers. Brand look: navy + gold, elk-crest logo.

**Meta ad account** owned by Alpine Pressure portfolio (user = Robby Hoffman, admin; Brandon Williams also admin). Timezone America/Denver, USD. Part of [[meta-ads-client-work]].

**Campaign built & PUBLISHED 2026-07-14 — status: "Preparing" (in Meta review, will go Active or Rejected):**
- Campaign "Graphic Engagement #1", **Engagement** objective, Auction, Messenger (Automatic destination = Messenger + Instagram).
- A/B test ON (test Creative, 7 days, compare by **cost per result**). Two ad sets, one image each, **$20/day** each (~$280 total):
  - "Locally owned, insured – Img A" = action shot (surface cleaner on driveway) + "PRESSURE WASHING" badge
  - "Locally owned, insured – Img B" = modern-home photo + **same** "Pressure Washing" badge (user chose a photo-only test, not the alt "Make Your Home Shine" badge that was also made)
- Audience: valley pins (Aspen +10mi, Basalt +10, Carbondale +10, Glenwood +15), age 35–65, All genders, no detailed interests, "people living in this location," audience expansion OFF. ~65–76k, flagged "Small" (expected/fine).
- 5 primary-text options + 5 headlines loaded (authentic: Carbondale/altitude/freeze-thaw, clean+seal, free quote). CTA = Send Message.

**Published.** To verify/finish: confirm the Messenger greeting/icebreakers are set (each ad's "Message template", or Page-wide via Business Suite → Inbox → Automations — may be using Page default). Watch for review approval ("Preparing" → "Active"), monitor Messenger + IG DMs, check cost-per-result day 3-4 but let the full 7 days run. Next round: before/after carousel as its own campaign.

**Known blocker:** Alpine Pressure Instagram ("Confirm access" for IG messages in Business Suite Inbox) is stuck in a login loop (login → captcha → bounce). Workaround: launch Messenger-only, or have the IG-login owner clear the security checkpoint in the IG app / use a clean browser with extensions off. IG DMs currently land in the IG app inbox without the auto-greeting until confirmed.

**Residential campaign result:** Img B (finished-home photo) WON — with more data settled at **$30.70/convo (5 convos, $153 spent)** vs Img A $47.95 (2 convos). Img A deleted/off. Img B is the residential winner.

**Carousel test (2026-07-24 → 27) — FAILED for messaging.** Built a 4-card residential carousel from Brandon's existing footage (cropped 1:1: clean-strip walkway, driveway soaping, luxury patio, walkway; files `~/Desktop/carousel-1..4-*.jpg`), Feeds-only (carousels break in Stories/Reels), RF + Vail audience, $20/day. Result: **2,249 impressions, $37.87 spent, 0 conversations.** Greeting/CTA were fine (verified). **LEARNING: carousels underperform for the Messenger objective — single strong image (Img B) is the residential winner. Use carousels only for traffic/website objectives, not messaging.** Turned Img B back ON 2026-07-27; will kill the carousel if still 0 by ~5k impressions.

**Geo expansion:** Brandon wants Vail + Beaver Creek added (residential too, not just commercial). One pin covers both: **Vail, CO +25mi** = Vail/Beaver Creek/Avon/Edwards/Eagle. Added to the carousel ad set. **Ads Manager gotcha:** the residential ad sets shared a saved audience ("Alpine Pressure"); editing it errors ("Not all ad sets can use this saved audience / Unknown error") because a COMPLETED ad set (Img A) references it. Fix: DELETE the completed ad set (turning off isn't enough — a paused ad set still references it), OR switch each active ad set to "Create new audience" to detach.

## COMMERCIAL CRM (started 2026-07-27)
Building a **standalone Streamlit CRM** for the commercial leads. It lives inside a new **Alpine company WAT workspace** at **`/Users/georges.patton/Alpine`** (its own git repo + future Railway service, NOT part of Wavecrest; structured like Wavecrest — root `CLAUDE.md` = blank WAT framework, `workflows/`, `tools/`, and the CRM app under **`Alpine/crm/`** with its own nested app-specific `CLAUDE.md`). User plans MORE Alpine projects here over time, each a subproject. Design spec: `Alpine/crm/docs/2026-07-27-alpine-commercial-crm-design.md`. Four jobs: work the outbound 553, catch inbound Meta form leads (Google-Sheet relay + import button, no webhooks), report pipeline, export Won+consent as the Lookalike seed. SQLite on a Railway **persistent volume** (disk is ephemeral) with all SQL behind `db.py` so Postgres later is a contained swap. Shared password gate (Robby + Brandon). Tables: `leads` + `activities`. Seed from `~/Desktop/Alpine-Commercial-Leads.xlsx`. Stages: New→Contacted→Interested→Quoted→Won/Lost.

**BUILT & MERGED to `main` 2026-07-27** (11 TDD tasks, per-task + whole-branch review, 36 tests passing). App runs end-to-end: `cd /Users/georges.patton/Alpine/crm && .venv/bin/python -m pytest` (36 pass); run locally with `CRM_PASSWORD=x streamlit run app.py` after seeding `python -m lib.seed data/Alpine-Commercial-Leads.xlsx`. **Real seed = 254 distinct leads** (287 tiered rows collapse via LOWER(name) dedupe — chain multi-locations like Burger King×3 merge to 1; enriched=15). **UI groups by CATEGORY (human labels), not numeric tiers** (changed 2026-07-30): the `category` column holds Property Mgmt / HOA (17), Hotels / Lodging (50), Apartments / Condos (13), Real Estate (referral) (19), Restaurants / Bars (155); Meta inbound → "Inbound", Leadfeeder → "Website". Fixed priority order via `db.CATEGORY_ORDER`/`sort_categories`/`category_counts`. Old numeric `tier`/`tier_label` columns kept for provenance but unshown; raw scraped categories (Food/bar, Office/lawyer) retired from UI. Seed + imports write the label into `category`. **Addresses backfilled 2026-07-30**: seed now joins the "All Leads (Raw)" workbook tab by name to fill street `address` + `city` (fill-only-empty, idempotent) → 181/254 leads have a street address, 237 a city; shown as a 📍 line in the Pipeline lead detail. The ~73 with no address had none in the OSM scrape — optional "Layer 2" is a Google Places API lookup (needs key + small cost) for the gaps, NOT yet built. **Quick-add card added 2026-07-30**: a "➕ Add a lead" `st.popover` at top-left of the Pipeline tab (below tabs, above filters) with a form (name* + category + city/address/phone/email/website/notes) → creates source="manual", stage="New", appears at top. Added a "Manual" category (in CATEGORY_ORDER after Website). 61 tests.
**"Alpine Field" visual theme applied 2026-07-30** (navy + gold brand, user picked it from 3 design-preview directions I mocked as an artifact). `crm/.streamlit/config.toml` (paper #F7F4EE / navy #12233F text / gold #C29A3F primary) + `crm/styles.py` (`inject()` global CSS, `header()` branded navy bar w/ gold crest + serif wordmark, `style_pipeline_df()` Styler color-coding the Stage column). Serif headings, gold tabs/buttons/KPI cards, em-dash for empty table cells, hides Streamlit chrome (Deploy/menu). Verified via Playwright screenshots (installed in venv only, NOT in requirements — dev tool). Design-direction artifact: https://claude.ai/code/artifact/72736650-d42d-48b6-84ca-93bf72301c56 Files: `crm/db.py` (all SQL), `crm/app.py`, `crm/screens/{pipeline,inbound,dashboard,lookalike}.py`, `crm/lib/{seed,sheets}.py`, `crm/config.py`, `crm/Procfile`, `crm/README.md`. Login = shared password env `CRM_PASSWORD`; DB path env `CRM_DB_PATH`; inbound sheet env `CRM_SHEET_ID`/`CRM_SHEET_TAB` + `GOOGLE_APPLICATION_CREDENTIALS`.

**Leadfeeder website-visitor ingestion ADDED & merged to `main` 2026-07-27** (51 tests). Second import button on the Inbound tab pulls site-visiting companies from a Leadfeeder Google Sheet (env `CRM_LEADFEEDER_SHEET_ID`/`CRM_LEADFEEDER_TAB`). Logic: match by website DOMAIN first (normalize_domain strips scheme/www/path) then name; NOT in CRM → add as New (source=leadfeeder, tier=Website); already in CRM → log a `website_visit` activity + bump to top, and promote to Interested ONLY if currently New/Contacted (never regress Quoted/Won/Lost). Counts per distinct lead (added/promoted/touched/skipped). Code: `db.import_leadfeeder`/`normalize_domain`/`touch_lead`, `lib/leadfeeder.py`. Still TODO to activate: create the Leadfeeder→Google Sheet export and set the env var.
**NOT yet done:** (1) deploy to Railway (add persistent Volume, mount, set `CRM_DB_PATH=/data/alpine_crm.db` + `CRM_PASSWORD`; README has steps); (2) no git remote yet (local repo only) — push to GitHub before Railway; (3) wire inbound: create the Zapier→Google Sheet flow, set `CRM_SHEET_ID`, finalize `map_row` aliases to the REAL Meta form columns (currently best-guess; can't yet capture contact_name separate from business name). Deferred code cleanups tracked in `crm/.superpowers` ledger (none blocking).

## COMMERCIAL EXPANSION (started 2026-07-24)
Client (Brandon) wants to add **Vail + Beaver Creek** (Eagle County) and target **commercial: hotels, apartments, HOAs, property managers** (recurring contracts >> one-off residential). Strategy agreed:
- **TWO commercial campaigns**, same audience + creative (clean form-vs-website test): (1) **Lead Form** (in-FB, direct submits) and (2) **Website Traffic** (drives to a Commercial/Request-a-Quote landing page). The website campaign lights up **Leadfeeder** (user already uses it for another company — IDs *which businesses* visit the site) + **Meta Pixel** (anonymous retarget audience + the future compliant **Lookalike seed**). Leadfeeder = named companies for outreach; Pixel = anonymous audience — different jobs, both off the same website traffic.
- **Lookalike is Phase 2:** no compliant seed yet, and any current seed is residential (wrong audience). Build the *commercial* Lookalike later from lead-form submitters + Pixel + WON customers (with consent). Do NOT upload the scraped list to Meta (consent/ToS) — scrape is for **direct outreach only**.
- Meta targeting = interest net (Property management, Commercial real estate, Facility management, Small business owners); the **scraped outreach list is the precision layer**, not a Meta audience.

**Lead-gen pipeline built (free OSM Overpass scraper + email extractor; scripts in scratchpad, offered to save to tools/):**
- `~/Desktop/Alpine-Commercial-Leads.xlsx` — tabs: Summary, **Outreach Pipeline** (17 Tier-1 property-mgmt/HOA targets pre-loaded, 12 with phone/email, Stage/Consent/Recurring dropdowns), Outreach (Tiered) 287 leads in 5 tiers, Tier 1 - Property Mgmt, All Leads (Raw) 553, Emails Extracted (93).
- `~/Desktop/Alpine-Commercial-Campaign-Brief.md` — full build brief: infrastructure (pixel/Leadfeeder/landing page), both campaign specs, 5 commercial primary texts + 5 headlines, lead-form questions, before/after creative brief, build checklist.
- **Two-sheet tracking plan:** Sheet A = Google Sheet "Meta Commercial Leads" (Zapier auto-flow from Meta lead form, like SameDayPsych) = the Lookalike export source; Sheet B = the Excel Outreach Pipeline tab (manual outreach) → copy WONs into Sheet A. Filter Sheet A to WON+consent → Custom Audience → Lookalike.
- **Commercial daily budget = $20/day.**
- **Campaign 1 (Commercial – Lead Form) BUILT to draft 2026-07-24** — Leads objective, $20/day ad-set budget, Lead Ads ToS accepted, ad set "Commercial – Interests" (both valleys via Avon+25mi & the Aspen-Glenwood pins, Advantage+ audience with hard min-age 25 + interests Property management/Commercial property/Facility management/Small business owners/Real estate). SAVED AS DRAFT — waiting on creative. Still to do when resuming: add hotel before/after creative, build the Instant Form (qualifying Qs: name, business/property name, email, phone, property type dropdown, # units dropdown), publish; then Campaign 2 (website traffic) after landing page + pixel + Leadfeeder are set up.
- **Creative plan:** Brandon is doing a HOTEL job next week (~early Aug 2026) → capturing real commercial before/after content there (shot list given: same-angle before/after of entrance/parking/pool deck/stairs, action shots, wide hero, horizontal+vertical). Waiting for that rather than using an interim residential image. Alpine's photo library is large (residential-heavy) if an interim is ever needed.

**Assets on `~/Desktop/`:** alpine-logo.png, alpine-profile-white.png / -blue.png (FB profile pic, 1080²), alpine-hero-trailer.jpg, alpine-og-image.jpeg (hero), alpine-badge.png (Aspen's 5★ Pressure Washing), alpine-badge-B.png (Make Your Home Shine — alt, unused). Need to SHOOT: commercial before/after (hotel/parking/breezeway/patio/pool-deck).
